#!/usr/bin/env sh
# Check proposed index bytes; AI_PYTHON can select a verified Python 3.13+.
set -e
PYTHON="${AI_PYTHON:-python}"
"$PYTHON" -c 'import sys; sys.exit(0 if sys.version_info >= (3, 13) else 2)'
"$PYTHON" scripts/ai/git_hooks.py pre-commit
