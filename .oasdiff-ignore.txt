# .oasdiff-ignore.txt — consciously accepted breaking changes
#
# Format: one entry per line, as produced by `oasdiff breaking --format text`.
# Every entry must have a corresponding ADR reference in docs/decisions/.
# This file is passed to check_breaking.sh via `--err-ignore`.
#
# How to add an entry:
#   1. Run `oasdiff breaking <base> openapi.yml` to get the exact change string.
#   2. Confirm the change is intentional and document it in an ADR.
#   3. Add the line below and reference the ADR.
#
# Current allow-list:

# --- ADR 0004 — version-prefix the auth paths (/auth/* -> /api/v1/auth/*) ---
# docs/decisions/0004-version-prefix-auth-paths.md
# Intentional ERR-level path rename; v0.2.0 (pre-1.0 breaking = MINOR bump).
# Both consumers (claude-django, claude-react-mui) update their auth base paths.
api POST /auth/login api path removed without deprecation
api POST /auth/logout api path removed without deprecation
api POST /auth/refresh api path removed without deprecation
api POST /auth/register api path removed without deprecation
api POST /auth/token api path removed without deprecation
