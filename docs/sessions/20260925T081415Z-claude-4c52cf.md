---
session: 20260925T081415Z-claude-4c52cf
agent: claude
recorded: 2026-09-25T08:14:15Z
branch: feat/p07-shared-memory
revision: f8e3162f67657ed2d2d333dfb8f86276f57368f5
---

# Session 20260925T081415Z-claude-4c52cf

<!-- Fill every section before committing. Record facts with their source; never
paste transcripts, env values, tokens or raw logs. -->

## Task

Close the P07 leftovers: one output-language preference shared by Claude and Codex (legacy migration) and a written runtime acceptance scenario.

## Changes

- Commit `f8e3162`: `project_state.py --language [--apply] [--keep-shared]`
  (legacy `.claude/rules/output-language.md` → `docs/ai/overrides/output-language.md`
  plus a pointer; a conflict writes nothing), the language shown in
  `session_context.py`, and a conflict reported as a finding.
- Docs: `project-state-migration.md` (row, section, ownership),
  `session-continuity.md` (the runtime acceptance run), `set-language.md`,
  `happy-path-author`.
- This record and the `docs/HANDOFF.md` entry.

## Decisions

- Shared preference `docs/ai/overrides/output-language.md`; the legacy
  `.claude/rules/output-language.md` becomes a fixed pointer, so exactly one
  writable preference remains, and old CLAUDE.md imports keep working.
- A conflict is never resolved by the tool alone: the user's current choice
  goes into the shared file, then `--language --apply --keep-shared` runs.
- The start context shows the effective language to every agent. An unmigrated
  legacy file is not a finding; a conflict is.

## Checks

- `python3.13 template-core/scripts/ai/generate_core.py --check` — PASS
- `python3.13 scripts/ai/core_sync.py --local-source --check` — PASS
- `python3.13 scripts/ai/production.py --check` — PASS
- `python3.13 scripts/ai/generate_adapters.py --check` — PASS
- `python3.13 -m unittest discover -s template-core/tests -p "test_*.py"` — 120 OK
- `python3.13 -m unittest discover -s tests -p "test_*.py"` — 27 OK
- Cloud Linux container, Python 3.13.13, Node 22.22.2, Git 2.43.0. Candidate `f8e3162`, base `e4c4f56`.

## Limitations

- Windows-native runs of `project_state --language` and `session_context.py`: NOT_VERIFIED
  (Linux fixtures cover CRLF/BOM and a cp1252 stream).
- The runtime acceptance run (Claude → Codex → Claude, parallel sessions) is written
  down but NOT_VERIFIED until someone executes it.
- Hosted CI for these commits: pending the push. The core pin is a development pin.

## Next step

The user publishes (`PUBLISH-P07-2026-09-24.ps1` Status → Push → UpdatePrs → Checks). Merge #65 → #66 only on the user's command. The runtime acceptance run follows docs/ai/session-continuity.md.
