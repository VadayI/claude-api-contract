---
session: 20260925T100942Z-claude-711334
agent: claude
recorded: 2026-09-25T10:09:42Z
branch: feat/p08-git-lifecycle
revision: 7d0571de2f36fa4f7e74864f35353a199ac3d69a
---

# Session 20260925T100942Z-claude-711334

<!-- Fill every section before committing. Record facts with their source; never
paste transcripts, env values, tokens or raw logs. -->

## Task

P08 (plan §10): implement the shared Git lifecycle G0–G9 in the family core, after the user's command of 2026-09-25 to merge P06/P07 and clean up Git first (done: contract main `db342b7`).

## Changes

- `7d0571d`: `template-core/scripts/ai/git_lifecycle.py` (inspect/report, commit, verify, share, merge, cleanup), `template-core/tests/test_git_lifecycle.py` (33 fixtures, bare remote + `tests/fake_gh.py`), `template-core/docs/ai/git-lifecycle.md`; optional `git.merge_method` in `project.schema.json`; local delivery to `scripts/ai/` and `docs/ai/`; `production-inputs.json`; contract wrap-up and git-operations use the CLI.
- This record and the `docs/HANDOFF.md` entry (committed with `git_lifecycle.py commit`).

## Decisions

- One stdlib CLI for all agents; states come from actual Git/gh state, the journal in the Git common dir is informational only.
- Default commit mode takes working-tree content of paths without staged changes; anything staged needs `--staged` (exact index).
- Merge only with `--expect-head` (full approved SHA); default method `merge` keeps stacked branches mergeable; CI mode and method are read from the base branch.
- Cleanup needs MERGED evidence plus inclusion proof; worktrees with ignored files are kept; remote delete uses a lease on the observed tip.

## Checks

- `python3.13 -m unittest discover -s template-core/tests -p 'test_*.py'` — 153 OK (git 2.43, Linux)
- `python3.13 -m unittest discover -s tests -p 'test_*.py'` — 27 OK
- `generate_core.py --check`, `core_sync.py --local-source --check`, `production.py --check`, `generate_adapters.py --check` — PASS
- Independent review agent: 17 findings, then 7 more on the rewrite; all fixed and pinned by fixtures (skipped checks, ignored files in worktrees, lease on remote delete, literal pathspecs, fork PRs, re-push after squash, base resolution, exit codes).
- Candidate: this branch on top of `db342b7` (contract main).

## Limitations

- NOT_VERIFIED: native Windows run of `git_lifecycle.py` and its fixtures; a real GitHub lifecycle through the CLI (gh JSON fields and exit codes are assumed from gh documentation and exercised only through `tests/fake_gh.py`); hosted checks on this branch (after push).
- The P07 runtime acceptance run (Claude → Codex → Claude) is still NOT_VERIFIED.

## Next step

User: publish the three P08 branches with `PUBLISH-P08-2026-09-25.ps1` (includes a Windows run of the lifecycle fixtures) and review the draft PRs; merge only on command, contract first, then repin Django/React to the integrated core.
