---
session: 20260925T075805Z-claude-93ac65
agent: claude
recorded: 2026-09-25T07:58:05Z
branch: feat/p07-shared-memory
revision: 6fb703abdced3f1f5c1f9c626e95ad924f5a2a5b
---

# Session 20260925T075805Z-claude-93ac65

<!-- Fill every section before committing. Record facts with their source; never
paste transcripts, env values, tokens or raw logs. -->

## Task

P07 remainder from IMPLEMENTATION-PLAN-2026-09-20 §9: documentation map, per-session records, agent-neutral start context and a mechanical continuity check, plus contract consumers.

## Changes

- Commit `6fb703a` (`feat/p07-shared-memory`):
  - core: `template-core/scripts/ai/session_context.py`, `core_paths.contained_entry`,
    documentation directories in `schema.project_links`, `docs/ai/session-continuity.md`,
    `tests/test_session_context.py` (15 tests);
  - contract: `ci_mode.py` writes an explicit documentation map, `session-start.sh`
    prints the context, AGENTS/handoff/wrap-up/living-plan/production-structure/
    environment/preflight updated, `test_production` fresh-seed continuity fixture.
- This record and the `docs/HANDOFF.md` entry.

## Decisions

- `docs/project-state/project.json` → `documentation` is authoritative; defaults
  cover projects without a map. Entries may only name documents or directories
  (no hidden, secret or configuration files).
- One record per session in `docs/sessions/` (`<UTC>-<agent>-<hex6>.md`, exclusive
  create); `docs/WORKLOG.md` stays as history; no `merge=union`.
- The newest record present in the working tree is the latest (merged records
  count). Relevant commits after it are a `--check` finding; a recorded revision
  missing from a clone (squash/rebase, shallow) is information only.

## Checks

- `python3.13 template-core/scripts/ai/generate_core.py --check` — PASS
- `python3.13 scripts/ai/core_sync.py --local-source --check` — PASS
- `python3.13 scripts/ai/production.py --check` — PASS
- `python3.13 scripts/ai/generate_adapters.py --check` — PASS
- `python3.13 -m unittest discover -s template-core/tests -p "test_*.py"` — 117 OK
- `python3.13 -m unittest discover -s tests -p "test_*.py"` — 27 OK
- `AI_PYTHON=python3.13 bash scripts/session-start.sh` — exit 0, context printed
- An independent review found 9 defects; all were fixed before the commit (BOM/CRLF,
  fenced headings, dirty record, record choice after a merge, squash/shallow revision,
  subdirectory root, LF on Windows, map restrictions, Checks in the output).
- Cloud Linux container, Python 3.13.13, Node 22.22.2, Git 2.43.0. Candidate `6fb703a`, base `0e3f4cd`.

## Limitations

- Windows-native run of `session_context.py` and the SessionStart hooks: NOT_VERIFIED
  (Linux only; the cp1252 stdout case is covered by a fixture).
- A real Claude → Codex → Claude session sequence on another machine: NOT_VERIFIED;
  only the mechanical fixtures ran.
- Hosted CI: not run — the branch is not pushed yet. The core pin is a development pin.

## Next step

The user runs `PUBLISH-P07-2026-09-24.ps1` (Status → Push → UpdatePrs → Checks). Merge #65 → #66 only on the user's command, then repin Django/React to the integrated core; after that, P08.
