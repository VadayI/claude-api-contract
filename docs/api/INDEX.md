# API index (human overview)

> The contract is `openapi.yml` (OpenAPI 3.1) at the repo root — the single source of truth. This index is a human-readable overview that points at it.

_No endpoints yet — the `spec/` first slice (auth + sample resource) lands in the next Etap. Generated/maintained by `docs-writer`._
