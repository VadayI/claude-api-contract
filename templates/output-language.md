# Output language

Always respond in {LANGUAGE_NATIVE}. Use {LANGUAGE_NATIVE} for explanations, status messages, PR descriptions, commit messages, and inline comments — except for code identifiers and tokens that must remain English (variable names, API paths, `operationId`s, schema/field names, log keys, error codes, file paths).
