# Local Claude settings (secrets, tokens) — do NOT commit
.claude/settings.local.json

# Node toolchain
node_modules/
dist/
tsp-output/
.tsp/

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log

# IDE
.idea/
.vscode/

# Local-only — NOT tracked (Ukrainian copy UA/, downloaded reference, personal notes)
/LOCAL/
# Session handoff — local only, not tracked
/HANDOFF.md

# Claude session-local state — regenerated by SessionStart hook (scripts/detect-env.mjs)
# and by /command logging. Must not be shared across machines or cloned into derived projects.
.claude/memory/env-detect.json
.claude/memory/command-log.jsonl

# Last reviewed/updated: 2026-06-06
